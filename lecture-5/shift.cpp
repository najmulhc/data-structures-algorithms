#include<iostream>
using namespace std;

int main(){
    // left shift (shift to the left side) makes the number almost double
    int left = 4 << 2;
    // right shift (shift to the right side) makes the number half
    int right = 19 >> 2;
   int n = 12 << 1;// 24
    cout << n << endl << right << endl;
    
    return 0;
}